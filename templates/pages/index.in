<h1><!-- tmpl_var name='uc_zone' --> - Fast and Reliable Dynamic DNS</h1>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-1 col-md-1">
    </div>
    <div class="col-sm-11 col-md-11">
      <p><!-- tmpl_var name='uc_zone' --> allows you to control DNS entries beneath the <code><!-- tmpl_var name='zone' --></code> domain.</p>
      <p>You can quickly and easily configure a name such as <code>gateway.<!-- tmpl_var name='zone' --></code> to point to the IP of your home gateway, whether that is an IPv6 address an IPv4 address, or both.</p>
      <p>You've can update your name(s) at any time via a simple call to <code>curl</code>, or using your web-browser, with no need to install or configure any client.</p>
      <p>To prevent abuse registration to this service requires a one-time fee of <b>&pound;5.00</b>.</p>
    </div>
  </div>
</div>

<h1>Git-based DNS Hosting</h1>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-1 col-md-1">
    </div>
    <div class="col-sm-11 col-md-11">
      <p>If you find this service useful you might wish to look at our sister-site, which allows <a href="https://dns-api.com/">publishing DNS records directly from a <code>git</code> repository</a>.</p>
      <p>It uses the same distributed and redundant backends, and allows you to update your records whenever you like, with fixed pricing based on the number of zones you host.</p>
      <p>Thanks to the use of <code>git</code> revision control system you maintain a complete history of your changes.</p>
    </div>
  </div>
</div>
